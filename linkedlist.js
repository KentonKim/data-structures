// linked list

// properties
    // size
    // head
    // tail

// methods
    // append(value) to end of list

    // prepend(value) to start of list

    // at(index) returns node atgiven index

    // pop() removes last element and returns

    // contains(value) 
        // returns true if value passed is in list
        // false otherwise

    // find(value)
        // returns index of node containing value
        // null if not found


    //insertAt(value, index)
        // inserts new node w value at given index

    // removeAt(index)
        // remomves node at given index

    // toString()
        // returns linkedlist object as string
        // format: ( value ) -> ( value ) -> ( value ) -> null
    
    